import { atom } from 'jotai';

export const butonActivateOnStepper = atom(true);

export const menuCollapsed = atom(false);

export const tipsViewAtom = atom(1);

export const isRutValidAtom = atom(false);

export const errorAtom = atom('');
